require 'spec_helper'

describe Jekyll3::Slim do
  it 'has a version number' do
    expect(Jekyll3::Slim::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
